# ============================================================
# PROGRAMA DE VENTAS POR SUCURSAL
# ventas[sucursal][anio]: promedios por sucursal, totales por
# anio y la sucursal con mayor promedio
# ============================================================

# TODO: funciones a implementar
# cargar_ventas() -> matriz (n x m) validada
# mostrar_tabla(ventas)
# promedio_por_sucursal(ventas) -> lista de promedios
# total_por_anio(ventas) -> lista de totales
# sucursal_max_promedio(ventas) -> (indices, promedio maximo)


def main():
    # TODO: cargar la matriz, mostrar la tabla, calcular los
    #       promedios y totales, y mostrar el resumen
    pass


if __name__ == "__main__":
    main()